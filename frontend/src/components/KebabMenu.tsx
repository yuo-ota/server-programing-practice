import { useState } from 'react';
import DotsIcon from '../assets/dots.svg?react';
import IconButton from './IconButton';

interface KebabMenuProps {
  className?: string;
}

const KebabMenu = ({}: KebabMenuProps) => {
  return (
    <div>

    </div>
  )
}